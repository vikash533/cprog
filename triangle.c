#include<stdio.h>
void main()
{
    int a,b,c;
    printf("enter the fist side of triangle\n");
    scanf("%d",&a);
    printf("\nenter the secon side of triangle\n");
    scanf("%d",&b);
    printf("\nenter the third side of triangle\n");
    scanf("%d",&c);
    if ((a==b)&&(b==c)&&(a==c)) 

    {
    printf("the givene triangle is equlaterral triangle");

    }
    
}