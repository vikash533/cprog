#include<stdio.h>
void main(){
    int cy,yoj,bonous,yr_of_ser;
    printf("ener the value of current year & year of joining");
    scanf("%d%d",&cy,&yoj);
    yr_of_ser=(cy-yoj);
    if (yr_of_ser>3)
    {
    (yr_of_ser>3);
    bonous=2500;
    printf("bonous = %d",bonous);
    }
    
}